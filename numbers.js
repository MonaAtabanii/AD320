/* The challenge:
        1. Create a file named numbers.js.
        2. Define a variable named example that references the integer 123456789.
        3. Use console.log() to print that number to the terminal.
*/

let example = 123456789;
console.log(example);