/* The challenge:
        1. Create a file named accessing-array-values.js.
        2. In that file, define array food : const food = ['apple', 'pizza', 'pear']
        3. Use console.log() to print the second value of array to the terminal.
*/

const food = ['apple', 'pizza', 'pear'];
console.log(food[1]);