/* The challenge:
        1. Create a file named number-to-string.js.
        2. Define a variable named n that references the number 128;
        3. Call the .toString() method on the n variable.
        4. Use console.log() to print the results of the .toString() method to the terminal.
*/

let n = 128;
n = n.toString();
console.log(n);