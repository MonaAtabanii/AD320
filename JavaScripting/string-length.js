/* The challenge:
        1. Create a file named string-length.js.
        2. Create a variable named example.
        3. Assign the string 'example string' to the variable example.
        4. Use console.log to print the length of the string to the terminal.
*/

const example = 'example string';
console.log(example.length);