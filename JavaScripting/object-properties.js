/* The challenge:
        1. Create a file named object-properties.js.
        2. Define a variable named food like this:
              const food = {
                types: 'only pizza'
              }
        3. Use console.log() to print the types property of the food object to the terminal.
*/

const food = {
  types: 'only pizza'
}
console.log(food.types);