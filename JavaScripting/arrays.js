/* The challenge:
    1. Create a file named arrays.js.
    2. Define a variable named pizzaToppings that references an array contains 3 strings in this order: tomato sauce, cheese, pepperoni.
    3. Console.log() to print the pizzaToppings array to the terminal.
*/

const pizzaToppings = ['tomato sauce', 'cheese', 'pepperoni'];
console.log(pizzaToppings);