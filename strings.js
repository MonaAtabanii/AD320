/* The challenge:
        1. Create a file named strings.js.
        2. Create a variable named someString like this:
                const someString = 'this is a string'
        3. Use console.log to print the variable someString to the terminal.
*/

const someString = 'this is a string';
console.log(someString);